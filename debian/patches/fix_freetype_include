Description: Fix the FreeType2 include in libgd.
 The embedded libgd code has an include directory that forces the freetype
 headers files to be in a freetype/ directory.
Author: David Martínez Moreno <ender@debian.org>
Forwarded: no
Last-Update: 2014-05-26

--- a/hphp/runtime/ext/gd/libgd/gdft.cpp
+++ b/hphp/runtime/ext/gd/libgd/gdft.cpp
@@ -63,7 +63,7 @@ gdImageStringFT (gdImage * im, int *brect, int fg, char *fontlist,
 #include "gdcache.h"
 
 #ifndef HAVE_FT2BUILD
-# include <freetype/config/ftheader.h>
+# include <config/ftheader.h>
 #else
 # include <ft2build.h>
 #endif
